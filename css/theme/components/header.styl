.header
    display: flex
    justify-content: space-between

    &-container
        position: sticky
        top: 0
        padding: (gap / 2) 0
        background: white

        body:not(.cms-home) &
            box-shadow: 0 0 2rem rgba(0, 0, 0, 20%)

        .scrolling &
            box-shadow: 0 1px 0 rgba(0, 0, 0, .1)